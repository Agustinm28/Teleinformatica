apiVersion: v1
kind: Service
metadata:
  name: metabase
  labels:
    run: metabase
spec:
  selector:
    run: metabase
  ports:
  - name: metabase
    port: 3000
    targetPort: 3000
  type: ClusterIP