apiVersion: v1
kind: Service
metadata:
  name: metabase-service
  labels:
    app: metabase
spec:
  selector:
    app: metabase
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP