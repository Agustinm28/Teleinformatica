apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: nginx # Cambiar a public si accedes a traves de una instancia con microk8s local
  name: uptime-ingress
spec:
  rules:
  - host: localhost # Aca cambiar host por dominio - UM -> amontana.my.kube.um.edu.ar
  - http:
      paths:
      - backend:
          service:
            name: uptime-service
            port:
              number: 3001
        path: /
        pathType: Prefix
